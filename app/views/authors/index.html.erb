<h2> List of Authors <h2>
<% AuthorsController::RUSS_alph.each do |l| %>
	<%= link_to l, list_authors_path(l) %>
<% end %>
<br>
<br>
<% AuthorsController::LAT_alph.each do |l| %>
	<%= link_to l, list_authors_path(@author, :letter => l)%>
<% end %>
<%= render 'form' %>
